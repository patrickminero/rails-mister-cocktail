<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.label :dose %>
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <%= f.submit %>
  <% end %>
  <%= link_to 'back to home', cocktails_path %>
</div>